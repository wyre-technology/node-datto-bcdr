/**
 * Common types shared across BCDR resources.
 */

export type Timestamp = number; // Unix epoch seconds

/**
 * A Unix epoch seconds value identifying a specific recovery point.
 */
export type EpochSeconds = number;
